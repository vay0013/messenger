spring:
  datasource:
    url: jdbc:postgresql://db:5432/messenger
#    url: jdbc:postgresql://localhost:5432/messenger
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
#  sql:
#    init:
#      mode: always
#      data-locations: classpath:

